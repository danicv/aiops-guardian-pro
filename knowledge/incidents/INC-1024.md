# INC-1024 — checkout-api OOM after release
A release reduced the checkout-api memory limit below normal working-set demand.

Evidence:
- OOMKilled
- repeated pod restarts
- error-rate increase immediately after deployment
- no material traffic increase

Resolution:
- rollback release
- restore validated memory limit
- add pre-production risk check for resource reductions
