import {useQuery,useQueryClient} from '@tanstack/react-query'; import {api,getApprovals} from '../api'; import RiskBadge from '../components/RiskBadge'
export default function Approvals(){const qc=useQueryClient();const {data=[]}=useQuery({queryKey:['approvals'],queryFn:getApprovals});async function decide(id:string,a:'approve'|'reject'){await api.post(`/api/approvals/${id}/${a}`,{decided_by:'approver@example.com',comment:`${a}d from Guardian portal`});qc.invalidateQueries({queryKey:['approvals']})}return <section><div className="title"><h2>Human approvals</h2><p>High-risk production changes pause until an authorized user decides.</p></div><div className="cards">{data.map((x:any)=><div className="panel" key={x.id}><div className="panelhead"><h3>{x.id}</h3><RiskBadge risk={x.risk}/></div><p><strong>{x.action}</strong></p><small>{x.investigation_id} · {x.environment}</small><p>Status: <strong>{x.status}</strong></p>{x.status==='pending'&&<div className="actions"><button className="primary" onClick={()=>decide(x.id,'approve')}>Approve</button><button onClick={()=>decide(x.id,'reject')}>Reject</button></div>}</div>)}{!data.length&&<div className="panel empty">No approvals yet. Run the default production investigation.</div>}</div></section>}
