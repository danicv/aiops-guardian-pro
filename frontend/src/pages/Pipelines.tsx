import {useQuery} from '@tanstack/react-query'; import {getPipelines} from '../api'; import RiskBadge from '../components/RiskBadge'
export default function Pipelines(){const {data=[]}=useQuery({queryKey:['pipelines'],queryFn:getPipelines});return <section><div className="title"><h2>Pipeline intelligence</h2><p>Release risk, failed-stage diagnosis and promotion decisions.</p></div><div className="panel"><div className="row head"><span>Pipeline</span><span>Status</span><span>Risk</span></div>{data.map((p:any)=><div className="row" key={p.id}><div><strong>{p.id}</strong><small>{p.application} · {p.version}</small></div><span>{p.status}</span><RiskBadge risk={p.risk}/></div>)}</div></section>}
